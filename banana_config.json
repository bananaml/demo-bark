{
    "version": "1",
    "inputs": [
        {
            "name": "prompt",
            "description": "The prompt to feed to the model.",
            "type": "string",
            "required": true,
            "default": "Let's try generating speech, with Bark, a text-to-speech model"
        }
    ],
    "output": {
        "audio": {
            "type": "audio",
            "source": "url",
            "path": "output"
        }
    }
}
